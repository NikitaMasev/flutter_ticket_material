name: ticket_material
description: A ticket view widget. Support all platform. There is standard click behavior.
version: 0.3.1
homepage: https://github.com/NikitaMasev/flutter_ticket_material

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter